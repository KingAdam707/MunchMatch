rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow any logged-in user (including anonymous) to read/write anything
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}